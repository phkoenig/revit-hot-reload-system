<Window x:Class="GeoJsonImporter.Work.UI.GeoRefDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="GeoRef - Adresse eingeben" 
        Height="250" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <TextBlock Grid.Row="0" Text="🗺️ GeoRef - Adresse eingeben" 
                   FontSize="16" FontWeight="Bold" Margin="0,0,0,20"
                   HorizontalAlignment="Center"/>
        
        <!-- Adress-Eingabe -->
        <TextBlock Grid.Row="1" Text="Adresse:" FontWeight="SemiBold" Margin="0,0,0,5"/>
        <TextBox Grid.Row="2" x:Name="AddressTextBox" Height="30" 
                 Text="Musterstraße 123, 12345 Musterstadt"
                 Margin="0,0,0,20"/>
        
        <!-- Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="OkButton" Content="✅ OK" Width="80" Height="30" 
                    Margin="0,0,10,0" Click="OkButton_Click"/>
            <Button x:Name="CancelButton" Content="❌ Abbrechen" Width="80" Height="30" 
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
